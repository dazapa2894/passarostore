console.log("holi2???");

document.querySelectorAll(".agregar").addEventListener("click", function () {
   console.log("shiii funssssioooonaaaaa! 2");

   /*
   console.log("value = " + $(this).attr(value));
   console.log("id = " + $(this).attr(id));

   fetch('/cart/add.js', {
       method: 'POST',
       headers: {
         'Content-Type': 'application/json'
       },
       body: JSON.stringify(formData)
     })
     .then(response => {
       return response.json();
     })
     .catch((error) => {
       console.error('Error:', error);
     });
   */
}, false);
